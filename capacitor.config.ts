import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.plataforma.lis',
  appName: 'PLATAFORMA-LIS',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
